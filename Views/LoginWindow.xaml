<Window x:Class="SneakerBackOffice.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SneakerBackOffice.ViewModels"
        Title="Login" Height="300" Width="400"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Email:" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="1" Grid.Column="1" Height="30" HorizontalAlignment="Stretch"/>

        <TextBlock Text="Password:" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center"/>
        <PasswordBox Name="PasswordBox" Grid.Row="3" Grid.Column="1" Height="30" HorizontalAlignment="Stretch"/>

        <Button Content="Login" Grid.Row="4" Grid.Column="1" Width="Auto" Height="40"
                HorizontalAlignment="Center" Command="{Binding LoginCommand}"/>

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                   Grid.Row="5" Grid.Column="1" HorizontalAlignment="Center"/>
    </Grid>
</Window>
